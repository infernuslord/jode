#!/bin/sh
for i in ci.all jcpp addHeader.pl make* COPYING TODO *.html doc/*.texi \
         *.java */*.java test/*.j
do 
    if test -w $i; then
	if [ $i != ${i%.java} ]; then
	    ./jcpp -DJDK11 $i 
	fi
	rcsdiff $i
	ci -u $* $i 
    fi
done
