#!/bin/sh
for i in ci.all jcpp addHeader.pl make* COPYING TODO *.html doc/*.texi \
         *.java */*.java */*.j
do 
    if test -w $i; then
	if [ $i != ${i%.java} ]; then
	    ./jcpp -DJDK11 $i 
	fi
	rcsdiff $i
	if [ -z "`rlog -L $i`" ]; then
	    rcs -l $i
	fi
	ci -u $* $i 
    fi
done
